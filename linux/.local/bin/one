#!/bin/sh

scriptDir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)

scale=${SCALE:-2}
app=${1:-foot}

echo ""
echo ">>> ONE is Starting..."
echo "Scale: $scale"
echo "App: $app"
echo ""

XKB_DEFAULT_LAYOUT=us \
XKB_DEFAULT_VARIANT=altgr-intl \
SCALE="$scale" \
cage -s -- sh "$scriptDir/one-session.sh" "$@"
