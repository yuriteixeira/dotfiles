#! env /bin/bash

profile=$1
profilesPath=$HOME/.config/sway/profiles/

if [ -f "$profilesPath/$profile/outputs" ];
then
  rm -rf $profilesPath/current
  ln -sf $profilesPath/$profile $profilesPath/current
fi

swaymsg reload
