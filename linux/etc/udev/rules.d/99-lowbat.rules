# Trigger low-battery handler on battery changes.
# The handler calculates aggregate capacity across all batteries, sends desktop
# notifications via the active user's DBus session, and hibernates at 5%.
ACTION=="change", SUBSYSTEM=="power_supply", KERNEL=="BAT*", TAG+="systemd", ENV{SYSTEMD_WANTS}+="low-battery@%k.service"
